<html>

<head>
    <title>Shoes mall</title>
    <%- include('externalink.ejs')%>
        <style>
            .login {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                padding: 10px;
                box-shadow: rgba(50, 50, 93, 0.25) 0px 6px 12px -2px, rgba(0, 0, 0, 0.3) 0px 3px 7px -3px;
                height: 400px;
            }

            .logoImage {
                display: flex;
                justify-content: center;
                margin-top: 20px;
            }

            body {
                background-image: url(https://wallpapercave.com/wp/atadv0w.jpg);
                background-size: cover;
            }
        </style>
</head>

<body>
    <div class="container" style=" position: relative ;width: 100%; height: 100vh;">
        <div class="logoImage">
            <h1 style="font-size: 40px; font-weight: bold; color: white;">E-Shop</h1>
            <h1>..</h1>
            <h1 style="font-size: 40px; font-weight: bold;  color: white;">User</h1>
            <h1>..</h1>
            <h1 style="font-size: 40px; font-weight: bold; color: white; ">Login</h1>
        </div>
        <div class="row ">
            <div class="login col-md-4 col-lg-4 ">
                <div class="text-danger" style="font-size: 20px; font-weight: bold;">Login Customer</div>

                <form id="form_ajax_validate" method="post" action="/signIn">
                    <div class="wrapper">
                        <div class="form-group mt-3">
                            <label style="font-weight: bold;">Email</label>
                            <input style="border-radius: 20px;" type="email" name="email" class="form-control" />
                        </div>
                        <div class="form-group">
                            <label style="font-weight: bold;">Password</label>
                            <input style="border-radius: 20px;" type="password" name="password" class="form-control" />
                        </div>
                        <div class="form-group " style="text-align: center;">
                            <button type="submit" class="btn btn-danger ">Sign In</button>
                        </div>
                        <hr>
                        <div class="form-group" style="display: flex; justify-content: end;">
                            <a href="/signUp">
                                <button style="border-radius: 20px;" type="button"
                                    class="btn btn-outline-danger ">Create Account</button>
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>

<script>

    var formAjaxValidate = document.getElementById("form_ajax_validate");
        var checkForm = function (e) {
        var form = e.target;
        if (this.email.value == "" || !this.valid_email.checked) {
            alert("Please enter a valid Email address");
            this.email.focus();
            e.preventDefault();
            return;
        }else  if (password == "") {
            alert("Please enter your password");
            password.focus();
            return false;
        }
        e.preventDefault();      
    }

    formAjaxValidate.addEventListener("submit", checkForm, false);
    formAjaxValidate.email.addEventListener("change", function (e) {
        if (this.value != "") {
            callAjax("checkEmail", this.value, this.id);
        }
    }, false);
     formAjaxValidate.password.addEventListener("change", function (e) {
        if (this.value != "") {
            callAjax("checkEmail", this.value, this.id);
        }
    }, false);

</script>

</html>